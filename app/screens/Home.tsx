// react, react-native
import {Text, View} from 'react-native';

import BasicsButton from '../components/BasicsButton.tsx';

const Home = () => {
  return (
    <View>
      <Text>홈 스크린</Text>

      <BasicsButton />
    </View>
  );
};

export default Home;
